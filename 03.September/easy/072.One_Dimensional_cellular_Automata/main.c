#include <stdio.h>
#include <stdlib.h>

#define SIZE 256

int main(void){

	int iterations, i;

	int board[SIZE];
	for(i=0; i<SIZE; i++)
		board[i] = 0;

	printf("How many iterations do you want to represent?: ");
	scanf("%d", &iterations);

	for(i=0; i<iterations; i++)
	{
		
	}

}
